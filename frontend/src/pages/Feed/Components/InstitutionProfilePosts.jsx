import React, { useState } from "react";
import PostCards from "./PostCards";
import Post from "./Post";

const InstitutionProfilePosts = ({ data }) => {
  const handleInstitutionClick = (e) => {
    console.log(e);
    e.preventDefault();
  };
  return <></>;
};

export default InstitutionProfilePosts;
